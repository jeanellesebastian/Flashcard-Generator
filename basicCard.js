// Create a BasicCard Constructor with two arguments: front and back

function BasicCard (front, back) {
    this.front = front;
    this.back = back;
}

console.log(" >>>>> BasicCard has LOADED!")

// Exports BasicCard
module.exports = BasicCard;
